const scriptEl = document.querySelector('[data-gesture]');

// if(scriptEl) {
//   const runtimeId = scriptEl.getAttribute('data-gesture');
//   window.GestureUrl = `chrome-extension://${runtimeId}/scripts/content2`;
//   console.log('[gesture]');
// }
